CREATE TABLE book (id INTEGER PRIMARY KEY, 
                   bib_key TEXT NOT NULL,
                   info_url TEXT,
                   preview TEXT,
                   preview_url TEXT,
                   thumbnail_url TEXT,
                   created_at TEXT NOT NULL);
